import { createUseStyles } from 'react-jss'

const useStyles = createUseStyles({
})

function Step4Specify() {
  const classes = useStyles()

  return (
    <div className='root'>
      Step4Specify
    </div>
  )
}

export default Step4Specify