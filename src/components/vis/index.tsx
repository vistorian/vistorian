import { createUseStyles } from 'react-jss'

const useStyles = createUseStyles({
})

function Vis() {
  const classes = useStyles()

  return (
    <div className='vis'>
      vis
    </div>
  )
}

export default Vis