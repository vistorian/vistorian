name: Deploy Vistorian 2.0 # name for your action

on:
  push: 
    branches:   #  Set a branch name to trigger action
      - master
    # paths:
    #   - dist/**
  # pull_request:  # perform action after pr

jobs:
  deploy:
    runs-on: ubuntu-latest   # action perform environment
    concurrency: ci-${{ github.ref }}
    steps: 
      - name: Checkout 🛎️
        uses: actions/checkout@v3

     - name: Pushes to another repository
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.TOKEN_VISTORIAN }}
        with:
          source-directory: 'dist'
          destination-github-username: 'shuxinhuan'
          destination-repository-name: 'vistorian2.0_dev'
          target-branch: main